# Triage labels

Use these canonical roles unchanged: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`.

Record the triage role in each local ticket's Status field. A ready-for-agent label does not override an outstanding human approval required by the bootstrap brief or project policy.
